// Prisma Schema for SYPE Ministry Admin Panel
// Database: PostgreSQL (Supabase)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Member {
  id          String   @id @default(uuid())
  name        String
  email       String   @unique
  phone       String?
  role        String   // "Student" | "Alumni" | "Admin"
  status      String   // "Active" | "Inactive" | "Pending"
  joinDate    DateTime @default(now())
  department  String?
  notes       String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("members")
}

model NewsArticle {
  id          String   @id @default(uuid())
  title       String
  author      String
  publishDate DateTime @default(now())
  excerpt     String
  body        String   @db.Text
  image       String?  // URL to image in Supabase Storage
  featured    Boolean  @default(false)
  category    String?
  tags        String[] // Array of tags
  views       Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("news_articles")
}

model Project {
  id           String    @id @default(uuid())
  name         String
  category     String    // "Documentary" | "Posters" | "Articles"
  topic        String
  description  String    @db.Text
  distribution String
  status       String    // "ongoing" | "completed" | "planned"
  year         String
  teamMembers  String[]  // Array of member IDs or names
  startDate    DateTime?
  endDate      DateTime?
  budget       Float?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  @@map("projects")
}

model Event {
  id           String    @id @default(uuid())
  title        String
  description  String    @db.Text
  date         DateTime
  time         String
  location     String
  category     String
  rsvpRequired Boolean   @default(false)
  rsvpCount    Int       @default(0)
  maxAttendees Int?
  attendees    String[]  // Array of member IDs or emails
  status       String    // "upcoming" | "ongoing" | "completed" | "cancelled"
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  @@map("events")
}

model Donation {
  id            String    @id @default(uuid())
  donorName     String
  donorEmail    String
  amount        Float
  currency      String    @default("RWF")
  date          DateTime  @default(now())
  type          String    // "one-time" | "monthly" | "project-based"
  projectId     String?
  paymentMethod String?
  paymentStatus String    @default("unpaid") // "paid" | "unpaid" | "installment"
  receiptSent   Boolean   @default(false)
  notes         String?   @db.Text
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  @@map("donations")
}

model FAQ {
  id       String @id @default(uuid())
  category String
  question String @db.Text
  answer   String @db.Text
  order    Int    @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("faqs")
}

model MediaFile {
  id          String    @id @default(uuid())
  name        String
  type        String    // "image" | "video" | "document"
  url         String    // URL to file in Supabase Storage
  thumbnail   String?   // URL to thumbnail in Supabase Storage
  size        Int       // Size in bytes
  uploadDate  DateTime  @default(now())
  category    String?
  tags        String[]  // Array of tags
  description String?   @db.Text
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@map("media_files")
}

model EmailCampaign {
  id         String    @id @default(uuid())
  subject    String
  body       String    @db.Text
  recipients String[]  // Array of email addresses
  sentDate   DateTime?
  status     String    // "draft" | "scheduled" | "sent"
  scheduledDate DateTime?
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt

  @@map("email_campaigns")
}

model Devotion {
  id                    String    @id @default(uuid())
  title                 String
  date                  DateTime
  excerpt               String    @db.Text
  content               String?   @db.Text
  image                 String?   // URL to image in Supabase Storage
  featuredVideoUrl      String?
  featuredVideoThumbnail String?
  featuredVideoTitle    String?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt

  @@map("devotions")
}

model ContactSubmission {
  id          String    @id @default(uuid())
  name        String
  email       String
  subject     String
  message     String    @db.Text
  status      String    @default("new") // "new" | "read" | "replied" | "archived"
  readAt      DateTime?
  repliedAt   DateTime?
  notes       String?   @db.Text
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@map("contact_submissions")
}

model Book {
  id          String    @id @default(uuid())
  title       String
  author      String?
  category    String    // "Health" | "Genzura" | "Ellen G. White Books" | "Bible" | "Inyandiko" | "Integuza" | "Others"
  description String?   @db.Text
  coverImage  String?   // URL to cover image in Supabase Storage
  fileUrl     String?   // URL to PDF/document in Supabase Storage
  isbn        String?
  publisher   String?
  publishDate DateTime?
  language    String?
  pages       Int?
  tags        String[]  // Array of tags
  featured    Boolean   @default(false)
  downloads   Int       @default(0)
  uploadDate  DateTime  @default(now())
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@map("books")
}

model CommitteeMember {
  id       String   @id @default(uuid())
  position String
  name     String
  church   String
  phone    String
  category String   // "leadership" | "team" | "auditor" | "asa_representatives" | "board_chancellors"
  image    String?  // URL to image in Supabase Storage
  email    String?
  order    Int      @default(0)
  active   Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("committee_members")
}
